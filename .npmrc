# Keep sharp's native/runtime deps resolvable at the top-level node_modules
# so Vercel serverless file tracing can include them reliably with pnpm.
public-hoist-pattern[]=*sharp*
public-hoist-pattern[]=*detect-libc*
public-hoist-pattern[]=@img/*
